
interface  props {
    title: string,
}

const CardProps = {
    title: 'rfergv',
}

export default function ListItem(props: props) {
    return (
        <div
         >
            <div>{CardProps.title}</div>
            <div>ewfderf</div>
         </div>
    );
}